// ==============================================================
// إعدادات الوصول — قم بتعديل القيم أدناه فقط
// ==============================================================

// البريد الإلكتروني المسموح له بالدخول
const ALLOWED_EMAIL = "karimaismail1998@gmail.com";

// كلمة المرور الافتراضية: clinic2026
// لتغيير كلمة المرور:
//   1) افتح Console في المتصفح على أي صفحة من التطبيق
//   2) نفّذ: await sha256("كلمة_المرور_الجديدة")
//   3) انسخ الناتج وضعه مكان القيمة التالية
const ALLOWED_PASSWORD_SHA256 =
  "ec189984c5b36432f04401eb55b916a4801153d10b627bc4cc590382e27d1aa8"; // سيتم استبدالها أدناه

// ==============================================================
// دالة تجزئة (SHA-256) — لا تعدّلها
// ==============================================================
async function sha256(text) {
  const buf = new TextEncoder().encode(text);
  const hashBuf = await crypto.subtle.digest("SHA-256", buf);
  return Array.from(new Uint8Array(hashBuf))
    .map(b => b.toString(16).padStart(2, "0"))
    .join("");
}
